# Prototypes

This folder indexes prototypes that have moved beyond idea intake.

For each prototype, record:

- repository name
- one-line thesis
- target user
- current stage
- validation signal

Recommended naming:

- `proto-<slug>`
